version: 2

models:
  - name: metricflow_time_spine
    description: Table calendaire requise par MetricFlow.
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day